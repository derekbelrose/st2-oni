---
name: "chatops.bounce_vmware_vm"
action_ref: "oni.bouncevm"
description: "Power off and on a VMware VM"
formats:
  - display: "bounce <vmname>"
    representation:
      - "bounce {{ vm_name }} on {{ vsphere }}"
      - "bounce {{ vm_name }}"
ack:
  format: "Executing `{{ actionalias.ref }}`, your id is `{{ execution.id[:2]...{{ execution.id[-2:] }}`"
result:
  format: |
    I have completed this action
